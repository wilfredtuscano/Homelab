# Vikunja secrets — copy to `.env` (gitignored) on cloud-ct and fill in.
#   openssl rand -base64 24   # POSTGRES_PASSWORD
#   openssl rand -base64 32   # VIKUNJA_SERVICE_SECRET (JWT signing key)

POSTGRES_PASSWORD=
VIKUNJA_SERVICE_SECRET=
